[project]
name = "security-log-openenv"
version = "0.1.0"
description = "Cybersecurity Log Analysis OpenEnv environment"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests"
]

[tool.openenv]
entrypoint = "app:app"